# 這是欄位名稱範例，不可填入真實帳密後上傳 GitHub。

# 上週 Google Sheet 版本（只供 AI 辨識舊資料來源）
GOOGLE_SHEET_ID=your-sheet-id
GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\service-account.json

# Workshop 6 資料庫由本機 Invoke-WithDbAccess.ps1 注入需要連線的子程序。
# 不要把下列欄位複製到 .env，也不要填入真實值後提交。
MEDTECS_DB_SERVER=
MEDTECS_DB_DATABASE=
MEDTECS_DB_SCHEMA=
MEDTECS_DB_AUTH=
MEDTECS_DB_USER=
MEDTECS_DB_PASSWORD=
MEDTECS_DB_ENCRYPT=
MEDTECS_DB_TRUST_SERVER_CERTIFICATE=
